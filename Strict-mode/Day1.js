'use strict';
let hasDriver=false;
const pass=true;
if(pass) hasDriver=true;
if(hasDriver)console.log("Paased")

//Sometime it happens like you did not any error in console for that we Use A line 'use strict ' mode if something went error in our code
//it will imedietely fetch that error and show the error